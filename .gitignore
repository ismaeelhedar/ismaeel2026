# اعتماديات
/node_modules
/.pnp
.pnp.*

# مخرجات البناء
/.next/
/out/
/build
*.tsbuildinfo
next-env.d.ts

# ملاحظة: نسخ الصور المحسّنة (public/photos/opt/) مرفوعة عمدًا مع الشيفرة.
# هي مولّدة، لكن رفعها يجعل المستودع مكتفيًا بذاته: استنساخ ثم بناء مباشرة
# بلا خطوات إضافية، وبلا صور مكسورة لو نُسي تشغيل npm run photos.

# بيئة ونظام
.env*
!.env.example
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
